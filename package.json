{
  "name": "growth-hack-lightning-talk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "for dir in 20*/; do marp --allow-local-files -I \"$dir\" -o \"$dir\"; done",
    "deploy": "npm run build && wrangler pages deploy . --project-name slides"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.2"
  }
}
